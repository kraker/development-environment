---
ansible-navigator:
  mode: stdout
  execution-environment:
    # Disable EE otherwise localhost target doesn't work
    enabled: false
    pull:
      policy: missing
  playbook-artifact:
    enable: false
