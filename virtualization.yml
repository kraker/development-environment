---

- name: Virtualization Host
  hosts: localhost
  become: true
  tasks:
    - name: Install "Virtualization Host" group
      ansible.builtin.dnf:
        name: '@Virtualization Host'
        state: present
