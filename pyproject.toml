[project]
name = "development-environment"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    # Needed for ansible-galaxy command
    "ansible-core>=2.20.5",
    "ansible-dev-tools>=26.4.6",
    "prek>=0.3.11",
]
